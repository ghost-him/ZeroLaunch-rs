---
description: 国际化规范 — 所有用户可见文本必须用 t()/$t()，语言文件放 i18n/locales/，支持 zh-Hans 和 en
condition: "t\\('|\\$t\\(|i18n/locales|i18n"
scope: "tool:edit(*.vue), tool:edit(*.ts), tool:write(*.vue), tool:write(*.ts)"
interruptMode: tool-only
---

# 国际化

- 所有用户可见文本 **必须** 使用 `t('key')` 或 `$t('key')`。语言文件放 `i18n/locales/`，支持 `zh-Hans` 和 `en`
