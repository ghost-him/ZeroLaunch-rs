# ZeroLaunch 冒烟测试手册

> 适用范围：2026-08-05 统一会话重构 + 键盘契约破坏性重构（声明式按键绑定 `bindings`、删除宿主兜底与 `claim_all`、`PanelKeyAction::Confirm` 三分支语义、`confirmQuery` 在途防重）后的运行时验证。
> 前置：`cargo test` / `bun run build` 已通过。本手册验证**运行时行为**（跨 IPC 契约、键盘分发、事件推送），静态测试覆盖不到的部分以此为准。
> 章节：A–F 会话/面板核心契约；G 发布前关键入口（内置命令/托盘/右键菜单/设置窗口）；H 窗口行为开关；I 配置联动与外观；J 快捷键与第三方插件。
> 预计耗时：P0 全量 25–30 分钟（A–F 15–20 分钟 + G/H 必测 5–8 分钟）；P1 项（H5–H8 / I3–I5 / J）可选，按时间预算取舍。

## 0. 前置准备

### 0.1 环境检查（每次测试前）

| 检查项 | 操作 | 通过标准 |
|---|---|---|
| 残留进程 | 任务管理器查 `zerolaunch-rs.exe` | 无（有则结束） |
| 端口占用 | `netstat -ano \| findstr "12345 8143"` | 无监听（vite 12345 / CLI 8143） |
| 构建产物 | 工作区根 `bun run tauri dev` | 无编译错误 |

### 0.2 启动与日志证据

`bun run tauri dev` 后按顺序等待以下日志出现（全部出现 = 启动正常）：

1. Vite dev server 监听 `12345`
2. Cargo 编译完成并启动 `zerolaunch-rs.exe`
3. 配置 / 插件初始化完成（内置组件注册无 error 级日志）
4. 搜索管道构建完成
5. CLI server 监听 `127.0.0.1:8143`
6. UI 窗口 / 托盘初始化
7. **`应用启动完成`**（最终标志）

> ⚠️ 若日志出现 schema 校验警告（`unknown setting` / `minItems` 违规等）→ 旧配置与新版不兼容。**不要删配置**，记录下来报告。

### 0.3 测试辅助约定

- **剪贴板验证**：所有「复制」用例，复制后必须 `Win+V` 或粘贴到记事本确认内容。
- **LLM 调用计数**：翻译相关用例的防重判据依赖日志——dev 控制台搜索 translator 的请求/响应日志（或日志目录）。「仅一次调用」指目标时间段内该请求只出现一次。
- **默认触发词**：translator = `fy` / `tr` / `翻译`；calculator = `=`。如用户配置改过，按配置替换。
- **内置命令触发词**（builtin-command-source）：`设置`/`settings`=打开设置、`刷新`/`refresh`=刷新数据库、`注册`/`reshortcut`=重新注册快捷键、`游戏`/`gamemode`=切换游戏模式、`退出`/`exit`=退出程序。
- **测试插件包**：J4/J5 需要准备一个第三方插件 `.zip`（可用 `plugin-template/` 构建或仓库内现成示例），安装/卸载全程不碰用户已有插件。

---

## A. 基础运行

| ID | 前置 | 操作 | 正确现象 |
|---|---|---|---|
| A1 | 启动完成 | 按全局唤醒热键（默认如 `Alt+Space`，以配置为准） | 搜索窗口从屏幕中心弹出，输入框自动聚焦 |
| A2 | A1 | 输入任意文本 | 候选列表随输入刷新（无报错、无闪烁） |
| A3 | A1 | 按 `Esc` | 窗口隐藏 |
| A4 | A3 | 再次按唤醒热键 | 窗口重新弹出，输入框**为空**（上次输入已清空，注意，在按esc时会先清空，后隐藏） |
| A5 | A1 | 窗口已打开时再次按唤醒热键 | 窗口隐藏，不崩溃、不产生第二个实例 | 
| A6 | 启动完成 | 任务管理器确认进程数 | 仅一个 `zerolaunch-rs.exe`（单实例生效） |

## B. 默认搜索宿主面板

| ID | 前置 | 操作 | 正确现象 |
|---|---|---|---|
| B1 | 窗口打开 | 输入 `notepad` → `Enter` | 记事本启动（Enter = 确认选中候选） |
| B2 | 窗口打开 | 快速连续输入 `a` → `ab` → `abc`（< 500ms/次） | 最终候选列表对应 `abc`；**无**旧结果回闪（revision 门控丢弃过期结果） |
| B3 | 窗口打开 | 输入内容，按 `↓` / `↑` | 选中项随箭头移动（不循环，候选列表无滚动（已知bug）） |
| B4 | 窗口打开 | 选中含多个动作的候选（如带「以管理员运行」的），按 `Tab` 与 `Shift + Tab` | 选中动作循环切换（当前动作高亮变化） |
| B4b | B4 | 选中候选后按 `Ctrl+1` / `Ctrl+2`（主键盘数字区） | 快捷执行第 1 / 2 个动作（如 `Ctrl+2` = 以管理员身份运行 → 弹 UAC）；数字小键盘不触发 |
| B5 | 窗口打开 | 输入内容后按 `空格`（spaceIsEnter 默认**关**） | 输入框出现空格（放行，不发起查询；后端按空格尾判断是否进入行内参数） |
| B5b | B5 后 | 设置 → 窗口行为 → 开启「空格确认」（spaceIsEnter）→ 输入内容后按 `空格` | 空格被宿主接管：发起**确认查询**（等同 Enter 语义，空格不进入输入框）→ 测试完恢复默认 |
| B6 | 窗口打开 | 输入内容 → 按一次 `Esc` | 输入框清空（第一步） |
| B7 | B6 后 | 再按一次 `Esc` | 窗口隐藏（第二步） |
| B8 | 窗口打开 | 输入不存在的关键词（如 `zzzznotexist`） | 候选列表正常显示，无报错 |
| B9 | 窗口打开 | 输入框**空**时按 `Enter` | 无操作 / 无崩溃（空查询被忽略） |

### B10. 行内参数（inline_param）

| 前置 | 操作 | 正确现象 |
|---|---|---|
| 存在带参数的候选（`{}` 占位） | 输入「候选触发词 + 空格」 | 进入参数输入态：搜索栏保留，显示参数输入提示 |
| 已进入参数态 | 输入参数值 → `Enter` | 参数确认，执行候选（程序带参启动） |
| 已进入参数态 | 按 `Esc` | 退出参数态，返回普通搜索 |
| 已进入参数态 | 回退 | 退出参数态，返回普通搜索 |
| 已进入参数态 | 输入普通文本 | 输入框正常接收（宿主按键不拦截字母键） |

### B11. 参数面板（param_panel）

| 前置 | 操作 | 正确现象 |
|---|---|---|
| 存在多参数候选 | 触发参数面板 | 显示参数表单，焦点在第一参数 |
| 参数面板内 | `Tab` / `Shift+Tab` | 焦点在参数间前后移动 |
| 参数面板内 | 填完参数 → `Enter` | 确认并执行 |
| 参数面板内 | `Esc` | 退出参数面板返回搜索 |

## C. 翻译面板 on_enter 模式（本次核心变更 ①）

> 前置：translator 设置为 on_enter 模式。四态面板：`empty`（用法提示）/ `ready`（"按 Enter 翻译"）/ 成功（含「复制译文」动作）/ `error`（失败）。

| ID | 前置 | 操作 | 正确现象 |
|---|---|---|---|
| C1 | — | 输入 `fy hello` | 面板出现，状态 **ready**：「按 Enter 翻译」，**不**自动翻译（无 LLM 调用日志） |
| C2 | C1 | 按 `Enter` | 发起翻译 → 面板显示译文（primary）与「复制译文」动作按钮 |
| C3 | C2（成功） | 按 `Enter` | **剪贴板 = 译文**（Confirm 三分支：有可执行动作 → 执行默认动作复制）。面板不崩溃 |
| C4 | C2（成功） | 按 `Ctrl+Enter` | 剪贴板 = 译文，且**无新翻译请求**（直接复制，不触发翻译） |
| C5 | 任意翻译面板 | 按 `Escape` | 返回默认搜索面板（GoBack），输入框保留可继续输入 |
| C6 | 面板 **error** 态（无凭据/网络失败，如输入 `fy @xx hello` 非法语言） | 按 `Enter` | 重新发起翻译（失败重试语义）；再次失败仍停留 error 态，不崩溃 |
| C7 | — | 输入 `fy`（无内容）或 `fy ` | 面板显示用法提示（empty 态，`用法: fy hello \| fy @en 你好 ...`），**无 LLM 调用** |
| C8 | C7 | 空面板按 `Enter` | 无操作 / 无 LLM 调用（空输入无害） |
| C9 | — | 输入 `fy @en 你好` → `Enter` | 按语言方向翻译（中文 → 英文），成功面板出现 |
| C10 | C2 成功后 | 修改查询为 `fy world` → `Enter` | 新译文替换旧译文；再 `Enter` 复制的是**新**译文（剪贴板不串旧值） |
| C11 | 触发词前缀 | 输入 `fy h`（部分文本）→ `Enter` | 按当前文本翻译（与 C2 同语义，验证不要求完整单词） |
| C12 | — | 输入 `tr hello`（第二触发词） | 同 C1：面板 ready，可 Enter 翻译（触发词均生效） |

## D. 翻译面板 live 模式（本次核心变更 ②：防重）

> 前置：translator 切换为 live 模式（设置 → 翻译 → 模式）。live = 输入防抖自动翻译。

| ID | 前置 | 操作 | 正确现象 |
|---|---|---|---|
| D1 | — | 输入 `fy hello`（不按 Enter） | 防抖（默认若干秒）后**自动**翻译，无需 Enter；成功面板含「复制译文」 |
| D2 | 面板自动翻译**在途**（译文未返回） | 快速按 `Enter` | **不产生第二次翻译请求**——日志中仅一次 LLM 调用（`panelQueryInFlight` 防重生效） |
| D3 | D1 成功 | 按 `Enter` | 剪贴板 = 译文（与 C3 同：有动作 → 复制） |
| D4 | 面板 **error** 态 | 按 `Enter` | 重新发起翻译（重试）；成功则面板恢复 |
| D5 | 任意 live 面板 | 按 `Escape` | 返回默认搜索 |
| D6 | D1 成功后 | 快速连续改输入 `fy he` → `fy hel` → `fy hell` | 防抖合并：仅对最终文本发起一次翻译（日志无多次重复请求） |
| D7 | D1 成功后 | 修改文本 → 自动翻译**在途**时按 `Ctrl+Enter` | 直接复制**当前已有译文**（若此前有），不触发新翻译；若从未成功过则无操作，不崩溃 |

## E. 计算面板（本次连带修正 ③）

> 触发词 `=`。计算同步完成，无「等待」态。面板形态：行内（保留搜索栏）。

| ID | 前置 | 操作 | 正确现象 |
|---|---|---|---|
| E1 | — | 输入 `= 1+1` | 面板立即显示结果 `2`（整数格式），含「复制结果」动作 |
| E2 | E1 | 按 `Enter` | **剪贴板 = `2`**（Confirm：有动作 → 执行默认动作复制） |
| E3 | — | 输入 `= 1.5*2` | 面板显示 `3`（整数值省略小数位，epsilon 比较） |
| E4 | — | 输入 `= (2+3)*4` | 面板显示 `20`（括号/优先级正确） |
| E5 | — | 输入 `= 1+`（不完整） | 面板显示错误提示（error 态），**无**「复制结果」动作 |
| E6 | E5 | 按 `Enter` | 不崩溃、面板保持（无动作 → 确认查询 = 重新计算，结果仍是错误提示） |
| E7 | — | 输入 `= `（单独） | 提示面板，不崩溃 |
| E8 | 任意计算面板 | 按 `Escape` | 返回默认搜索面板 |
| E9 | E1 成功后 | 修改为 `= 2*10` → `Enter` | 结果更新为 `20`；再 `Enter` 复制的是 `20`（剪贴板不串旧值） |

## F. 插件面板通用契约（声明即接管 / 放行）

> 本组验证键盘契约的**边界语义**——命中声明键 = 宿主接管；未命中 = 放行给输入框，宿主不解释、不兜底、不拦截。

| ID | 前置 | 操作 | 正确现象 |
|---|---|---|---|
| F1 | 任意插件面板（翻译/计算） | 按字母键 `a`、`x` 等 | 字符进入搜索输入框（未声明键放行），输入框光标正常移动 |
| F2 | 任意插件面板 | 按 `Backspace` / `Delete` | 正常编辑输入框内容（放行） |
| F3 | 任意插件面板 | 按 `↑` / `↓` | 无动作且不报错（未声明键放行；若输入框有历史则走浏览器行为） |
| F4 | 翻译面板（C2 成功态） | 按 `Ctrl+C` | 不拦截（放行），无宿主动作 |
| F5 | 翻译面板 → `Esc` 返回搜索 → **再次**输入 `fy hello` 重入面板 | 重入后面板内按 `Enter` | 按键契约仍生效（面板重入后 session-state 重推，bindings 不丢失） |
| F6 | 插件面板 | 连续快速按 `Enter` ×3 | 无重复动作：有动作时只复制一次（防重），无动作时只发起一次查询 |
| F7 | 插件面板与宿主面板切换 | 面板内 `Esc` 返回搜索后，按 `Enter` | 回到**宿主** Enter 语义（启动选中候选），不残留插件按键契约 |
---

## G. 内置命令 / 托盘 / 右键菜单 / 设置窗口（发布前必测 P0）

> 内置命令候选由 `builtin-command-source` 提供，触发词见 0.3。确认后经 `AppCommand` 通道执行（`ShowSettings` / `RefreshCandidates` / `ReregisterHotkeys` / `ToggleGameMode` / `ExitProgram`）。
> 结果右键动作来自后端 `get_actions`（Path 目标三动作：「打开」「以管理员身份运行」「打开所在文件夹」）。

| ID | 前置 | 操作 | 正确现象 |
|---|---|---|---|
| G1 | 窗口打开 | 输入 `设置` → `Enter` | 设置窗口弹出并聚焦（可正常操作） |
| G2 | G1 | 关闭设置窗口（标题栏 X） | 设置窗口**隐藏**而非销毁（进程不退出、托盘仍在）；再执行 G1 能重新弹出 |
| G3 | 窗口打开 | 输入 `刷新` → `Enter` | 后端日志出现「AppCommand: 候选项刷新完成，共 N 个」；无崩溃 |
| G4 | 窗口打开 | 输入 `注册` → `Enter` | 日志出现「快捷键重新注册成功」；随后按唤醒热键仍有效 |
| G5 | 窗口打开 | 输入 `退出` → `Enter` | 程序退出（进程结束、托盘图标消失）。⚠️ 建议放最后执行，或执行后重启再继续 |
| G6 | 启动完成 | 右键托盘图标 | 菜单项齐全：设置窗口 / 刷新数据库 / 重新注册快捷键 / 游戏模式（复选框）/ 退出程序 |
| G7 | G6 | 点击「设置窗口」 | 同 G1 |
| G8 | G6 | 点击「重新注册快捷键」 | 同 G4 |
| G9 | G6 | 勾选「游戏模式」→ 再取消勾选 | 勾选后**唤醒热键失效**（全部快捷键注销）；取消后热键恢复（日志「快捷键已恢复」） |
| G10 | 启动完成 | 双击托盘图标 | 窗口显示/隐藏切换（隐藏→显示并聚焦；显示→隐藏） |
| G11 | 窗口打开 | 搜索 `notepad`，在结果项上右键 | 菜单含「打开」「以管理员身份运行」「打开所在文件夹」；点「打开所在文件夹」→ 资源管理器打开 notepad.exe 所在目录 |
| G12 | G11 | 结果右键 → 点「以管理员身份运行」 | 弹出 UAC 提权（同意后以管理员启动；任务管理器可见 elevated）；无 UAC 且未启动 → FAIL |
| G13 | 窗口打开 | 搜索栏空白处右键 | 菜单含「刷新数据库」「打开设置」；点「刷新数据库」→ 通知「数据库已刷新，共 N 个候选项」 |
| G14 | 窗口打开 | 点底部 Footer 齿轮按钮 | 设置窗口弹出（隐藏主窗口，按鼠标所在显示器居中） |

## H. 窗口行为配置开关（P0: H1–H4；P1: H5–H8）

> 前置：设置 → 窗口行为。默认值：ESC 优先隐藏=关、唤醒时重置会话=开（通用设置）、激活失败时启动新实例=开、启用窗口拖动=关。

| ID | 前置 | 操作 | 正确现象 |
|---|---|---|---|
| H1 | 窗口打开 | 开启「ESC 优先隐藏」→ 输入 `fy hello` → 按 `Esc` | 窗口**直接隐藏**（不再先清空输入）——与 B6/B7 默认行为分支不同 |
| H2 | H1 后 | 关闭「ESC 优先隐藏」→ 输入文字 → `Esc` ×2 | 第一次清空输入、第二次隐藏（默认行为，= B6/B7） |
| H3 | 窗口打开 | 输入 `fy hello` 进入翻译面板 → **点击窗口外部**（失焦自动隐藏）→ 重新唤醒 | 恢复初始搜索界面：无面板残留、输入框为空（reset_session_on_wake 默认开） |
| H4 | H3 后 | 关闭「唤醒时重置会话」→ 进入翻译面板 → 失焦隐藏 → 重新唤醒 | 翻译面板状态保留（输入内容与面板状态保持，reset_plugins=false 分支）→ 测试完恢复默认 |
| H5 | 已运行程序 | 打开记事本 → 搜索 `notepad` → `Enter` | 前台激活已运行的记事本窗口（不启动新实例） |
| H6 | H5 后 | 关闭「激活失败时启动新实例」→ 重复 H5 | 激活失败时**不**启动新实例（无新进程），日志含 ActivationFailed 降级处理，不崩溃 → 测试完恢复默认 |
| H7 | 窗口打开 | 开启「启用窗口拖动」→ 拖动窗口到屏幕角落 → 隐藏 → 唤醒 | 窗口回到拖动后的位置（window_position_x/y 已持久化），而非默认定位 |
| H8 | H7 后 | 关闭「启用窗口拖动」→ 拖动 → 隐藏 → 唤醒 | 位置不被记忆（回到鼠标跟随/居中默认定位）→ 测试完恢复默认 |

> H5/H6 依赖「激活失败」可复现（如窗口被全屏程序遮挡/拒绝激活）；若当前环境无法构造失败场景，可 SKIP 并注明。

## I. 配置变更实时联动 + 外观（P0: I1–I2；P1: I3–I5）

> 联动链路：设置保存 → `ConfigEvent::SettingsChanged` → SessionDispatcher 重建候选/搜索管道，搜索窗口**无需重启**。

| ID | 前置 | 操作 | 正确现象 |
|---|---|---|---|
| I1 | — | 设置 → 自定义命令 新增 {名称 `mycmd`，命令 `C:\Windows\System32\calc.exe`} → 关闭设置 | 不重启、不手动刷新，搜索 `mycmd` → 候选项出现；`Enter` 启动计算器 |
| I2 | I1 | 删除该命令 → 关闭设置 → 搜索 `mycmd` | 候选项消失（配置联动移除） |
| I3 | — | 设置 → 外观 → 主题改为「深色」→ 关闭设置 | 搜索窗口**立即**变深色（无需重启）；再改回「浅色」立即恢复 |
| I4 | I3 | 外观 → 主题 `system` / `light` / `dark` 循环 | 每次保存立即生效；`system` 跟随 Windows 深浅色设置 |
| I5 | — | 外观 → 语言改为 `en` → 保存 | 设置窗口 UI 与**托盘菜单文字**变英文（update_menu_language 联动）；改回 `zh-Hans` 恢复 |

## J. 快捷键自定义 + 第三方插件管理（P1，可选）

> 前置：J4/J5 需要测试插件 `.zip`（见 0.3）；J1–J3 会短暂改变全局热键，测完必须恢复。

| ID | 前置 | 操作 | 正确现象 |
|---|---|---|---|
| J1 | — | 设置 → 快捷键：唤醒热键改为 `Ctrl+E` → 保存 | 旧键 `Alt+Space` 失效；按 `Ctrl+E` 能唤醒搜索窗口 |
| J2 | J1 | 改回 `Alt+Space` → 保存 | 恢复默认；`Alt+Space` 生效 |
| J3 | — | 先让其他程序占用 `Ctrl+E` → 热键改为 `Ctrl+E` | 注册冲突有 warn 日志、不崩溃；解除占用后托盘「重新注册快捷键」能恢复 |
| J4 | 测试插件就绪 | 设置 → 第三方插件管理 → 从本地文件安装测试 `.zip` | 列表出现新插件（状态运行中）；搜索其触发词 → 候选项出现（PluginRegistered → 重建候选管道） |
| J5 | J4 | 卸载该插件 | 列表移除；再搜索其触发词 → 候选项消失（Unregistered → 重建候选管道） |

---

**冒烟通过判据**：A–J 全部用例 PASS（SKIP 需注明原因，如功能未配置、环境无法构造激活失败）。任一 FAIL → 进入异常定位。
